type Query {
    getUsers: [User]
    getShops:  [Shops]
    getCurrentUser: User @hasRoles(roles: [
        user
        admin
    ])
    getProducts(paginate: PaginationInput):ProductListRes @hasRoles(roles: [admin user anonymous])
}