type Query {
    getUsers: [User]
    getCurrentUser: User @hasRoles(roles: [
        user
        admin
    ])
    getProducts(paginate: paginationInput):ProductListResData @hasRoles(roles: [admin user anonymous])
}